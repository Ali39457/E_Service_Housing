import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ServiceProvider() {
  return (
    <View>
      <Text>ServiceProvider Dashboard</Text>
    </View>
  )
}

const styles = StyleSheet.create({})